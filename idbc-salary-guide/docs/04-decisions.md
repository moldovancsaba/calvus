# IDBC Salary Guide — decision register

*Numbered, dated, with who decided and what it replaced. Reconstructed on 2026-09-18 from
the dated entries in `SOURCES-AND-GAPS.md` and the commit history; from here on, a new
decision is added here first. "Owner" is the studio side; "client" is IDBC.*

| # | Date | Decision | By | Why / what it replaced |
|---|---|---|---|---|
| D1 | 2026-07-30 | Bérezés and Expert Pool become standalone pages; the trends page keeps the survey only | owner | one combined page had grown past what a reader could hold; also the spec's page split |
| D2 | 2026-07-30 | Empty salary columns are hidden rather than shown as dashes | owner | a column of "–" reads as missing data |
| D3 | 2026-07-31 | Ordinal answer scales keep their order; 0 %-response options are hidden everywhere | owner | zero-respondent options presented as findings |
| D4 | 2026-08-17 | Adopt the client's `3_SAP` mockup as the layout of all three subpages; build the point-line TOP3 chart from it as a shared component | owner | replaced the static range cards; the mockup was the client's own design |
| D5 | 2026-08-17 | The salary band's two ends are labelled "Vállalatok által kínált bér" / "Jelöltek által elvárt bér" per the mockup, although the sheet holds one range | owner | a labelling choice, recorded as gap 6 with its risk; the neutral "Min./Max." was rejected |
| D6 | 2026-08-18 | "Összes" (later "Összesített adatok") is a pseudo-segment, always first and default; segment options are computed per topic | owner | landing on a real segment with zero respondents |
| D7 | 2026-08-24 | Two topics renamed and the EU pay-transparency question moved to the end of Bérezés | client | editorial |
| D8 | 2026-08-24 | Eleven-area filter on the trends page with a per-area summary block; the summaries do not filter the survey until re-tabulated data exists | client ask, owner scope | honest skeleton over a fake filter |
| D9 | 2026-08-25 | Questions with no data under a real segment are hidden outright; the aggregate is never shown in their place; one panel-level sentence remains | owner, refining the client's per-question message | the client saw the message version live and the owner simplified |
| D10 | 2026-08-25 | The stacked whole-sample chart is the only rendering of the two 1–4 scale questions; the mean-bar variant is rejected | owner | "the stacked chart is the good one" |
| D11 | 2026-08-25 | The area dropdown is replaced by eleven area links opening a dedicated area page from one template; area texts move to `areas.json` | owner, after client feedback | one renderer for eleven areas instead of eleven copies |
| D12 | 2026-08-25 | Until per-area data exists, area pages show the matching edition's pooled sample and say so | owner | never claim area-level data that does not exist |
| D13 | 2026-08-25 | The client's final area names and texts are adopted; five labels renamed | client | `SG_Területi_összefoglalók_final.docx` |
| D14 | 2026-08 | The Bérsávok table returns to neutral Minimum / Maximum headers; the chart keeps the stakeholder labels; the client's "valós piaci adatok" note is shipped as their copy | client | recorded next to gap 6 |
| D15 | 2026-09-01 | Area pages read the client's per-area re-tabulation; the pooled stand-in (D12) is retired | owner, on client data | closes the client's 2026-09-01 feedback on missing segment filters |
| D16 | 2026-09-08 | The final workbook replaces the survey half; `datasets` replaces editions/answers; the two 1–4 questions show the weighted average only; the aggregate is labelled "Összesített adatok" | client (data and labels), owner (schema) | "ez már a teljesen végleges kutatási anyag" |
| D17 | 2026-09-15 | Below 600 px of container width the salary tables render as cards; Bérsávok groups collapse behind a header, SAP rows do not | owner | 419 px of sideways overflow on a phone |
| D18 | 2026-09-16 | SAP cards collapse like Bérsávok'; the panel behind the mobile cards is dropped, the cards stay white; the TOP3 chart gets a narrow one-block-per-position layout with its own value scale under every chart | client (workshop) | corrected once by the client the same day |
| D19 | 2026-09-18 | Header on every page: Piaci trendek · Bérek · SAP · Expert Pool · Esettanulmányok, Ajánlatkérés and Kijelentkezés right-aligned; Ajánlatkérés links to `idbc.hu/ajanlatkeres/` pending confirmation | client (menu), owner (target) | the client named the item, not the target |
| D20 | 2026-09-18 | Area media: seven areas get a video placeholder, four a key-thought box; the Finance infographic slot is retired | client | their list of which area gets which |
| D21 | 2026-09-18 | Expert Pool becomes the Expert Community page with the client's copy; tiles per the client's PNG; join and contact forms inert | client (copy, tile), owner (inert forms) | |
| D22 | 2026-09-18 | TOP3 rows carry a LinkedIn pill and a footnote; counts matched by an explicit table, never guessed; a renamed position fails the build | owner | the first pass showed zero pills because the old bértábla did not match |
| D23 | 2026-09-18 | The salary data is rebuilt from the client's current bértábla (13 areas, 432 rows, 39 standalone TOP3 rows); area codes get the names already used on the trends pages; Sales and Marketing stay split | owner, on client data | the Talent Insight list was cut to this table |
| D24 | 2026-09-18 | The Bérek and SAP tables always show and include the TOP3 rows | owner, reverting the same day's exclusion | the owner saw a missing table on Retail; the table is the complete reference |
| D25 | 2026-09-18 | Esettanulmányok is a conventional article, not a card grid | client and owner (workshop) | the first pass had not been briefed |
| D26 | 2026-09-18 | Expert Pool tile bars align to the card bottom so tiles with and without a market line line up | client note | |
| D27 | 2026-09-18 | Standard documentation structure; technical package written with every stack decision in status PROPOSED | owner | see `11-architecture.md` §11; the flip of any ADR is the next D-number here |
| D28 | 2026-09-20 | A **responsible-data policy record** for the guide (SSOT §6b, PROPOSED): the registration checkbox is a notice acknowledgement, not marketing consent; B2B role addresses by legitimate interest, named persons and candidates by consent; salary data stays aggregated; the privacy notice must be real before registration runs — rules R12–R14 | The studio, on the owner's directive (responsible data for every client) and the hub audit | docs only; no page changed (fine-tuning rule) |
| D29 | 2026-09-21 | **The bértábla sheet is the single data source**: the LinkedIn Talent Insight counts are columns on `WEB_BERTABLA_IMPORT` and `EXPERT_POOL_IMPORT` (`linkedin_talent_insight`, plus `talent_insight_pozicio` where the report's label differs); the converter reads them from the sheet and the separate Talent Insight workbook is retired to a fallback | owner | the owner edits one sheet and feeds it back; nothing lives only in the prototype |
| D30 | 2026-09-22 | **Client feedback triaged: only the studio's own prototype scope is touched.** The client's mail mixed feedback on this static prototype with feedback on a real backend/CMS the studio does not build (account registration storage, a rich-text editor, an "editable pages" admin list, live Excel-to-site sync) — those items are named but left unbuilt here, flagged back to the client rather than faked. Everything else fixed in one round: registration subtitle copy; hero crop (`object-position: center` → `center 20%` site-wide — the fixed 400 px-tall hero band was cropping heads at wide viewports); the Piaci trendek chart-intro copy; the two-tile "Bérsávok/Expert Pool" CTA's width; the Ajánlatkérés header button now opens a new in-guide `kapcsolat/` page instead of leaving the guide; every green summary tile and the Excel-download CTA removed from Bérek and SAP; SAP's technológiai trendek prose rebuilt as tiles (reusing the existing `.range-card` component) with its own contact card, recruitment trends kept as a collapsible text block with a second contact card; the TOP3 chart's per-position sub-label dropped the redundant, already-selected area code (client: "an extra gray row... two position names") down to just the experience level; Bérek's further-down table drops the "Tapasztalati szint" pseudo-label on mobile entirely, blank instead of "–" where no level exists; the home-office "current policy" question kept in its source/ordinal order instead of being re-sorted by percentage | owner, triaging the client's mail | `08-client-asks.md`, `19-implementation-prerequisites.md`, `SOURCES-AND-GAPS.md` |
| D31 | 2026-09-22 | **Two area-name taxonomies, not one — verified separate, not reconciled.** The client's mail gives two different, overlapping-but-distinct area-naming lists: one for Piaci trendek (`areas.json`, e.g. "Pénzügy, Számvitel", "Ügyfélszolgálat, Adminisztráció") and a differently-worded one for Bérek (`build-salary-data.py`'s `AREA_NAME`, e.g. "Pénzügy és számvitel", "Office Support") — confirmed by re-reading the client's own two separate bullet lists side by side, not assumed. Both were applied verbatim to their own page rather than picking one wording and spreading it to both. Renaming `areas.json`'s display names required a bridge field (`dataKey`, holding each area's pre-rename name) so `terulet/index.html`'s join into `guide-data.json`'s `datasets` object (keyed by the old names) kept working — caught and fixed in the same round, before it could silently fall back 8 of 11 areas to the pooled aggregate | owner, cross-checking the client's two lists | `data/areas.json`, `terulet/index.html`, `data/build-salary-data.py` |
| D32 | 2026-09-24 | **Expert Pool's size legend reinstated — the client's second look reversed D30's removal.** After seeing the legend gone, the client asked to keep it after all; the raw IDBC Expert Community headcount is dropped instead, so only the bar and the LinkedIn Talent Insight count show. Piaci trendek's recruiting-strategy question (base view and the company-size breakdown) now keeps the client's own answer order instead of being sorted by percentage — the same class of ordinal-order bug as D30's home-office fix, on a different question. Terulet sub-pages get a plain green header band instead of a photo hero, per the client's request. (This round's commit, `e59dfc2`, shipped without a documentation update at the time — recorded here retroactively on 2026-09-25 so the register stays complete; CLAUDE.md's documentation-ships-with-the-change rule applies regardless of which session makes the commit.) | client, second review | `expert-pool/index.html`, `index.html`, `terulet/index.html` |
| D33 | 2026-09-25 | **TOP3 chart label collision, fixed at the algorithm, not patched per-row.** The client reported labels "skewing" when TOP3 values sit close together (desktop and mobile). Verified with the actual rendered SVG coordinates, not assumed: the old label-declutter cascaded strictly left-to-right from the leftmost dot, so a tight 3-value cluster dragged its 2nd and 3rd labels 40–90px clear of their own dots. Replaced with a min/max-cascade average (run the same push left-to-right, then mirrored right-to-left, average the two per label) — for well-spaced values this equals the old, unchanged result; for a tight cluster it centers the group on its natural midpoint instead of dragging it rightward. One function (`layoutLabels` in `assets/top3-chart.js`), so the fix reaches every page that loads it (Bérek, SAP, Expert Pool) and both the wide and the mobile "compact" chart layout. Found and fixed in the same pass: the wide chart's row-label column was a fixed 280px regardless of the position name's actual length — BSC's "Supply Chain / Order Management Specialist" measures ~298px at that font, so its lowest-value dot sat on top of the label's last letters whenever that value landed at the domain minimum (an artifact of a long name, not of close values, but visible in the same chart the client was looking at) — the column width is now sized to the longest position name in view, never narrower than the old 280px floor | owner, verified against live-rendered SVG coordinates | `assets/top3-chart.js` (asset version bumped to `v=8`) |
| D34 | 2026-09-25 | **"Sales Project Manager" showing "Manager" under it at BSC is real data, not a bug — confirmed, not guessed.** The client asked whether this is Drive-editable. Traced directly: `Sales Project Manager`'s `szint` (level) column in the bértábla sheet is literally `Manager`, read verbatim by `build-salary-data.py` with no transformation, and rendered as the TOP3 chart's sub-label — this is exactly what D30 changed that sub-label to show (the level, once the redundant repeated area code was dropped). Answer: **yes**, editable directly in the Drive sheet — blanking that row's level column and re-running the converter removes it, no code change needed. Left as-is; a code-side rule to suppress a level that merely repeats a word already in the position name was considered and rejected as guessing at intent for one row rather than fixing a reported defect | owner, tracing the data path before answering | none — data-only question |
| D35 | 2026-09-25 | **Live sync built (ADR-2's upgrade path) — and a real break in the converter found and fixed before it could ship.** The client asked for live (or on-demand) sync to the `IDBC_bertabla` sheet. Before building the sync, the sheet was actually read (its public export, since the sheet is shared "anyone with the link can view/edit") — not assumed compatible — and the converter hard-failed against it: the sheet's own `terulet` column had been retyped by hand at some point to hold the site's full display names ("Business Service Center (BSC)") instead of the old short codes (`BSC`) `build-salary-data.py` expected, with two further spelling variants ("Pénzügy és Számvitel", "Pharma, Life Science" singular). Per the owner's explicit instruction, **the sheet was corrected to the prototype's canonical naming** (Find & Replace, scoped to `WEB_BERTABLA_IMPORT`, match case + whole-cell: 42 + 15 cells), not the other way around; the converter was separately made resilient to either naming style (`AREA_ALIASES`/`area_name()`, case/whitespace-insensitive) so a future hand-edit doesn't silently break the pipeline again. A second break, unrelated to naming: `EXPERT_POOL_IMPORT` now carries a `Non-IT / Qualified Person` row with no headcount — the converter previously crashed on this; it now skips an incomplete row and prints it rather than inventing a number or blocking every other row. Re-running the corrected converter surfaced a real, substantial content update already sitting in the sheet: Expert Pool's entire category system replaced (15 rows, old Finance/Pharma-specific roles → 29 rows, a new IT/Non-IT taxonomy) and 3 SAP/Építőipar position names reworded — applied to `guide-data.json` as the client's real current data, not held back. A new tab, "HIÁNYZÓ ADATOK - kérés", added directly to the live sheet requesting the two genuine remaining gaps in the client's own words: IT Contracting salary rows, and the missing Qualified Person headcount | owner, live-verified against the actual sheet at every step | `data/build-salary-data.py`, `data/guide-data.json`, `.github/workflows/idbc-sync-bertabla.yml`, the live `IDBC_bertabla` sheet |
| D36 | 2026-09-25 | **IDBCSYNC backfilled: every value the prototype uses, fully exploded, one row each.** The owner added an `IDBCSYNC` tab to `IDBC_bertabla` (columns `id`, `változó`, `érték`, `megjelenés`, `segítség`) and set it as the single sheet — asked twice to confirm granularity, the answer was explicit: everything, no exceptions, no pointer rows to other tabs. 13,160 rows: 114 static page texts/labels/contacts/form fields, 33 area fields (`areas.json`), 30 SAP catalogue entries, 1,470 salary values (every present field of every `webBertabla` row), 58 Expert Pool values, 39 Talent Insight counts, 11,402 survey values (every simple answer, every weighted score, and every cell of the experience/company-size breakdown matrices), 14 meta rows (`siteMap`, `filterDimensions`). IDs are deterministic and index-free where the data allows (`SALARY-<existing record id>-<FIELD>`, `POOL-<iparag>-<pozíció>`, `SURVEY-<area>-<side>-<base/cross>-Q<n>-O<n>` or `-S<segment>-G<answer>`), so a future sheet → site converter can map rows back. **Every value is stored as text, deliberately**: imported as numbers, 96 answer-bucket labels (`1-5`, `6-10`, `11-15`) would have become dates, the footer phone `+36 30 479 0090` a formula error, and 1,300+ decimals like `12.5` locale-ambiguous in a Hungarian sheet. Method: generated from the repo's own data, served for minutes from this project's GitHub Pages (a gist was refused by the session's data-exfiltration guard; the data is already public on the live site), pulled in with `IMPORTDATA` + `REGEXEXTRACT` (text-only), frozen with Paste special → values, verified cell by cell against the source (0 mismatches, 0 formulas left), then the transport files removed from the repo. The owner granted the sheet's external-data permission from a signed-in session (an anonymous editor cannot) | owner (scope, twice confirmed), studio (method) | the live `IDBC_bertabla` sheet, `IDBCSYNC` tab |
| D37 | 2026-09-25 | **IDBCSYNC is the single source of every text and figure on the guide, synced every 15 minutes.** Owner directive: use `IDBCSYNC` as the SSOT for all data, sync every 15 minutes, hide the other tabs, hide the rows nobody needs to edit, hide the technical `id` column. D36's rows described the site but could not rebuild it (area summaries cut at 200 characters, respondent counts only inside help text, lists packed into one cell), so the tab was rebuilt as one editable value per row: 17,637 rows, 4,266 left visible (every page text, area texts, the bértábla, Expert Pool, the SAP catalogue, survey questions and answer labels) and 13,371 hidden (survey figures, screen-reader labels, technical keys). Every visible text on the eight pages carries `data-sync="ID"` (attributes `data-sync-attr`, phone and e-mail links `data-sync-href`), every on-screen text in the page scripts and `assets/top3-chart.js` a `/*sync:ID*/` marker; header and footer texts are one row for all pages, and the home tiles, the Piaci trendek dropdown and the area pages read the area's one name row. `data/idbcsync.py` rebuilds `guide-data.json`, `areas.json` and the page texts from the sheet's export and replaces both old converters (`build-guide-data.py`, `build-salary-data.py`, removed; the unused `talentInsightTop3`, `readme`, `siteMap`, `filterDimensions` blocks and `areas.json`'s `dataKey` bridge went with them). Survey labels are stored once per question, percentages once per cell, the respondent count once per segment — the "(N fő)" tooltip count is derived from them and reproduces all 1,846 source rows exactly. Proven before switching: rebuilt from the rows, the data had zero differences in any survey payload, salary row or area text, and 584 home filter combinations, 11 area pages, Bérek (every area, 1440 and 390 px), SAP and Expert Pool rendered the same HTML as the live site. Intended differences only: the Piaci trendek "Szegmens" dropdown shows the areas' current names in tile order (it still showed the names from before 2026-09-22); two trailing spaces in Expert Pool position names trimmed; Bérek and SAP split on a fixed area key (`kod`), so renaming the SAP area in the sheet cannot move SAP rows onto Bérek. A sheet error (a date where text belongs, a word in a salary, an unknown `{placeholder}`, a missing row) stops the sync before anything is written and names the row; every value is HTML-escaped. Flagged to the owner: the sheet is editable by anyone with its link, so anyone holding the link can change the live guide within about 15 minutes. |
| D38 | 2026-09-25 | **idbc.hu becomes the reference for the prototype's look; plan in `15-idbc-hu-alignment.md`; Phase 0 applied.** The owner asked to learn idbc.hu and plan the prototype's alignment with it. Measured, not assumed: the colours were already idbc.hu's (the client's demos came from it); the gap is the typeface (Gilroy vs Inter), the type scale, and the shell and components (header, hero, cards, buttons, forms, footer). The plan keeps the data components as the client designed them and restyles them with idbc.hu's tokens only; six decisions come first (typeface licence, which design wins where idbc.hu and the client's demos differ, header and footer content, photography). The owner approved publishing the plan and Phase 0: the footer's phone (+36 30 479 **8**090) and floor (**13.** emelet) corrected in IDBCSYNC to idbc.hu's. Found on the way: a phone typed with a leading `+` became a formula and exported as `#ERROR!`, which the sync would have published as text — no run happened in between; the converter now refuses Sheets error values and the phone row's help text says to type `'+36 …`. |
| D39 | 2026-09-25 | **The prototype takes idbc.hu's design; the alignment plan delivered in one pass.** The owner asked to deliver `15-idbc-hu-alignment.md`; its open decisions were taken as recommended — Outfit standing in for Gilroy until IDBC confirms the licence (idbc.hu serves Gilroy without cross-origin headers, so it cannot be borrowed), idbc.hu for the page frame and the client's demos for the data components (to confirm with the client), idbc.hu's header shape with the guide's links, idbc.hu's full footer, stock photos until IDBC's own arrive. One shared stylesheet (`assets/site.css`) and menu script replace the shell every page repeated; the pages keep their own rules only. The chart and pool colours moved to idbc.hu's greens. Proven before going live: all 1,142 rendered chart/table/tile states show the same text and figures as the live site; no page scrolls sideways at 375 or 1440; no console errors. The new footer texts are IDBCSYNC rows, added to the sheet before the pages so the 15-minute sync never failed. |
| D40 | 2026-09-25 | **The missing survey questions were missing from the topics, not from the data; corrected in IDBCSYNC.** The client's feedback: on the employer side, Home office lacks "Milyen hatással van a jelenlegi home office policy a toborzás sikerességére?" in every area, and AI lacks "Rendelkezik a vállalatod dedikált költségkerettel AI fejlesztésekre vagy kezdeményezésekre?" and "Mire használja a vállalatod AI eszközöket a toborzási folyamat során?" outside IT. Read from the sheet, not assumed: the three questions (employer Q13, Q09, Q08) have text, answer labels and figures for all 12 data sets. Q09 and Q08 were only in the IT + Contracting question set's AI topic, and Q13 was in no topic at all (its rows said "jelenleg egyik témában sem"). The 2026-09-22 answer (C-7, "absent from the parsed data") was wrong. Four hidden technical rows added at the end of `IDBCSYNC`: Q13 as the 3rd Home office question in both sets (`SURVEY-TOPIC2-SET1/SET2-EMPLOYER-03`), and Q09 then Q08 as the 4th and 5th AI questions in the general set (`SURVEY-TOPIC3-SET1-EMPLOYER-04/05`), in the client's order. Q13's seven "megjelenés" cells now say "téma: Home office". No code change: the sync already builds topics from these rows. Checked on a fresh export (27 cells changed, nothing else), then on the rendered pages: 560 employer charts across 12 data sets, every company size and both topics, all with figures, none missing; area pages the same | client feedback, traced in the sheet | the live `IDBC_bertabla` sheet, `data/guide-data.json` (via the sync) |
| D41 | 2026-09-25 | **TOP3 labels that crowd each other stack upward, over their own dots.** After D33 the client still saw close amounts spread sideways (BSC: 600 000 / 630 000 / 650 000) and asked for "valamelyik csússzon feljebb picit". `layoutLabels` in `assets/top3-chart.js` no longer shifts labels sideways: each amount stays centred over its dot (moved in only at the chart's edges), and a label that would touch one already placed goes up one row, as often as needed. Label rows are 13 drawing units apart on the wide chart and 17 on the phone chart, whose drawing grew from 84 to 104 units tall to fit three rows. Measured on the rendered SVG for all 12 Bérek areas and SAP at 390 and 1440 px: no label overlaps another, none leaves the chart, none covers a dot, and a label is never more than 20 units off its dot (only at the edges on phones). `top3-chart.js?v=12`, `chart.css?v=9` (its tooltip font follows the page font) | client, second look at the chart | `assets/top3-chart.js`, `assets/chart.css`, the pages that load them |
| D42 | 2026-09-25 | **The survey rows and the header and footer rows are hidden in IDBCSYNC.** Owner directive. Hidden where they stand, nothing moved or changed: rows 3–59 (`SHARED-HEADER-…`, `SHARED-FOOTER-…`, 57 rows) and the whole survey section, rows 111–386 (its heading, the total label, the four topic names and every `SURVEY-EMPLOYEE/EMPLOYER-…` question and answer label, 276 rows). Row 2 stays visible because it carries the tab's instructions, as do rows 60–66 (the TOP3 legend, the LinkedIn note and pill, and two survey chart messages, which are shared labels, not header or footer). Visible rows: 4,290 → 3,957. Checked on a fresh export: 333 rows newly hidden, exactly those; 0 values changed; the sync reads the export with 0 files changed, so the site is untouched. Editing a hidden text now means showing its rows first (the arrows at the row numbers) | owner | the live `IDBC_bertabla` sheet |

## Standing rules that came out of these

- No figure is ever invented; an inert control is shown with an "unavailable" treatment and a reason (D2, D9, D12, D22).
- The client's own copy ships as written, even where it asserts more than the source data does — and the divergence is recorded (D5, D14).
- One template per repeated thing (area pages, tiles, chart) — never eleven copies (D11).
- In fine-tuning, only the reported item changes; nothing is restructured or hidden unasked (D24, D25).
- Client feedback naming a backend/CMS capability the prototype cannot have (accounts, a rich-text editor, live data sync) is flagged back, never faked with static content pretending to be dynamic (D30).
- Every text and figure on the guide comes from the `IDBCSYNC` sheet tab; change the sheet, not the page — a hand edit to a marked text is overwritten by the next sync (D37).
