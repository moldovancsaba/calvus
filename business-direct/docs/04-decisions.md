# business.direct — decision register

| # | Date | Decision | By | Why / what it replaced |
|---|---|---|---|---|
| D1 | 2026-09-19 | The product is named **business.direct** | owner | — |
| D2 | 2026-09-19 | Three views: platform operator, listed business (provider), consumer (family) | owner | DiscountDirect's seller/buyer pattern plus the platform role, because the platform runs the machine across listings |
| D3 | 2026-09-19 | Four automations clickable in v1: digests and alerts; activation; generated landing pages and SEO; provider campaigns | owner | all four chosen |
| D4 | 2026-09-19 | Real data pulled by a converter from a platform's public API; nothing edited by hand | owner | over a fictional instance |
| D5 | 2026-09-19 | Sibling of DiscountDirect: its SSOT terms, GDS token names and decided stack (D26) are reused | owner | over a standalone concept |
| D6 | 2026-09-19 | The shape is the **one-person sales and marketing machine**: departments as automations, a knowledge layer, human-in-the-loop, **optional AI support** per department, an **intelligence dashboard**, **integrations** (the platform's API first) | owner, with two reference videos | widens D3 |
| D7 | 2026-09-19 | The prototyping standard applies from stage 0: no page before the design system is approved | standard | `PROTOTYPING.md` §2 |
| D8 | 2026-09-19 | Tokens: GDS role names shared with DiscountDirect; product accent teal `#0f766e` | Calvus, proposed | siblings share a system and differ by one accent |
| D9 | 2026-09-19 | **Gate 1 approved** — tokens and components as on `design-system.html` | owner: "Approved" | D8 stands; components moved to `assets/components.css` as the one source for the page and every frame |
| D10 | 2026-09-19 | **Everything product- and client-facing is in English** | owner | replaced the Hungarian UI copy of the first design-system page; docs were already English |
| D11 | 2026-09-19 | **Your Field NYC (getyourfield.com) is the first client**; *Most én sportolok!* and job portals / classifieds are reference ideas | owner | replaced D4's choice of the Hungarian instance as the data source; that data moved to `data/reference-sportolok/`; a second converter of the same shape pulls Your Field |
| D12 | 2026-09-19 | No "Calvus Hub" link on any user-facing page or document, in any project | owner | removed from every generator, renderer and hand-written page repo-wide |
| D13 | 2026-09-19 | Demo personas: provider **Brooklyn Force Soccer** (Park Slope, explicitly unclaimed, e-mail and phone on the card, Saturday sessions, trial by appointment); family: a Park Slope parent with two children | Calvus, on the data | the owner asks #2–3 default to these until answered |
| D14 | 2026-09-19 | **Two primary flows**: (1) **B2C social communication** — posting listings, updates and news to social channels to acquire new families for the listing page; (2) **B2B sales flow** — contacting listed businesses and inviting them to apply to manage their own listing, then more. Digests and alerts, generated pages and provider campaigns are the second ring | owner | reorders D3/D6: the machine's front door is acquisition on both sides; the frames are built around the two flows |
| D15 | 2026-09-19 | Two components added for D14 — the **social post card** (channel, listing, copy, media slot, state) and the **pipeline stage strip** (identified → contacted → replied → applied → managing → upgraded) — PROPOSED, reviewed with gate 2 | Calvus | the approved set had no social or pipeline component |
| D16 | 2026-09-19 | **Gate 2 approved** — the four layout frames and `05-layout-specs.md` as issued; build starts | owner: "Approved and Continue" | D15's two components enter the approved set |
| D17 | 2026-09-19 | The prototype is **one page, three views** (`index.html` + `assets/app.js`): the role switch and a persona select in the top bar; every screen rendered from `data/providers.json`; approvals, pipeline stages, replies, saves and preferences live in memory and reset on reload; nothing sends, publishes or connects | Calvus | one page keeps the frames' shell and the state in one place; a per-view page set would need shared state anyway |
| D18 | 2026-09-19 | **Sample activity is generated from the real catalogue, not invented rows**: social drafts come from providers that have an image and an announcement, a trial offer or a next session; the invitation goes to the 130 providers with an e-mail on their card; the three sample replies carry real provider names and our invented words; the family digest is her saved providers plus real Park Slope providers with a published next session | Calvus | keeps "what is real / what is sample" checkable (`03-sources.md`) |
| D19 | 2026-09-19 | The technical package (`10`–`14`) is written on **DiscountDirect's decided stack as PROPOSED ADRs** (Next.js 15, MongoDB Atlas, Vercel, Upstash, Blob, Resend, DoneIsBetter SSO), business.direct as a **sibling app** with **one platform-connector interface** for Your Field and the reference instance, the **outbox as the only sender** and **families without accounts here** (ADR-1 to ADR-8) | Calvus, PROPOSED | the client platform runs on the same family (`02-audit.md` §1); the owner or client flips an ADR and the flip is the next D-number |
| D20 | 2026-09-19 | **Round 1 accepted as tested** — the owner clicked through the prototype and the presentation with no correction ("Great tested"); round 2 closes the two items the build log left inert on purpose: the sequence and reply editors, and the tablet width measured | owner | ask #1 closes; the next build round waits on a new ask or a flipped ADR |
| D21 | 2026-09-19 | **Assumed until the owner answers asks #4–6**: v1 integrations are the platform API, e-mail (Resend), Instagram + Facebook and platform push — TikTok, X, SMS, Google Business Profile and calendars come later; the base machine is **bundled by the platform**, the provider **buys reach** (the platform's own products: featured listing, camp placement, local discovery profile — sample prices); the prototype stays **read-only** against the platform | Calvus, assumed | the owner said continue with no answers; each assumption is one line to flip |
| D22 | 2026-09-19 | **Phase 2 — provider campaigns and upgrades**: the provider view gains *Campaigns* (trial, open spots, announcement, registration — built from the card, audiences sample, approve / edit / skip, sent by the family's preferences under the cap) and *Results*; upgrades move a provider to *upgraded*; the family inbox shows campaigns from saved providers with the reason | owner: "continue with the next phase" | completes D3's fourth automation (provider campaigns) and the pipeline's last stage |
| D23 | 2026-09-19 | **The intelligence recap is its own screen** on the platform (week, by department, market radar from the catalogue, needs-you), computed from real data and this session's approvals; the overview keeps the tiles | Calvus | D6's "intelligence dashboard" was only tiles on the overview |
| D24 | 2026-09-19 | **`09-business-logic.md`** joins the set as the one narrative of the rules (parties, flows, departments, campaigns, money, families, law, recap, what the machine never does); the SSOT stays the term register | owner: "business logic … everything" | DiscountDirect has the same document; the standard's slot 09 was free |
| D25 | 2026-09-19 | **Phase 3 — conversations on both sides**: the provider view gains *Conversations* (family enquiries by platform message, e-mail and missed call, each with an answer drafted from the provider's knowledge files; approve / edit / skip; reply-time and missed-call tiles from state); a family can *Ask about a trial* from Saved and the enquiry appears in that provider's inbox, the approved answer in her inbox; on the platform, a published post receives a comment with a drafted reply ("someone tags you — first, not a queue"). The provider's "Waiting for you" now shows enquiries, not the platform's own replies to the provider | owner: "Great Continue" | research §2 (speed to lead) was the provider's strongest value and had no screen; the family → provider → family loop is now clickable |
| D26 | 2026-09-19 | **Analytics and unit economics are part of the product**: an *Economics* screen on the platform computes CAC and LTV for providers, the marketing value of an avid family, content ROI and the next-dollar ranking from the catalogue, this session's pipeline and editable assumptions; `16-analytics-and-unit-economics.md` is the model, with the events to log, attribution and rules R16–R19; the provider's results carry a "your return" line | owner: "ROI calculations and planning for CAC and LTV for B2B clients, marketing value for the avid users, and all the other analytics … data-driven marketing decisions" | the first finding the model produces is that outbound e-mail to a 252-provider catalogue does not pay for itself on $49 upgrades (LTV : CAC 0.7 at the defaults) — the base machine is the platform's growth engine, or the catalogue grows, or ARPA rises |
| D27 | 2026-09-19 | **The decision flow is client-facing**: the presentation carries "How the machine decides" (events → nightly metrics → next dollar → a person approves → measure and adjust) and the first finding; research III documents the cases and practices each step rests on and adds P10–P12 | owner: "update the client presentation with the data driven decision flow … industry research … best case studies and best practices as a source of our services" | the client sees the loop, not only the screens |
| D28 | 2026-09-19 | **The research is implemented** — P1–P12 enter the business logic and the prototype in the form that fits the system best: a media department behind one adapter interface with the clip engine as v1 and generation as "later"; real-media / generated labels on every post with C2PA at creation; the weekly anchor plan per neighbourhood × activity; AI-citation readiness per generated page; the public neighbourhood newsletter; sending infrastructure with warm-up, cap and bounce limits and a three-touch sequence ordered by a **propensity score**; the upgrade card gated by delivered value (R19); a neighbourhood holdout as the running experiment; cohort LTV by channel that stays "assumption" until 100 observations; voice kept "later" | owner: "implement the results of the research into our business logics and prototyping based on the best possible outcome" | closes asks #9 and #11; rules R15, R20–R22 join the SSOT |
| D29 | 2026-09-19 | **The business logic is audited** (`17-business-logic-audit-and-swot.md`): 18 findings, two critical (the family cap as written is consumed by the digest alone — no campaign or alert could ever send; the CAN-SPAM footer lacks a postal address), eight major, eight minor; a SWOT with evidence per item; twelve recommendations Q1–Q12 PROPOSED. The audit changed nothing except two factual errors in our own documents (phone-only 72 / neither 50 were swapped) and two stale sequence descriptions | owner: "a deep business logic audit … a professional SWOT" | Q1–Q12 wait for the owner; asks #12–13 added |
| D30 | 2026-09-19 | **The audit's recommendations Q1–Q12 are adopted** and implemented: the family cap counts provider-originated messages only (the platform's digest and alerts run on preference); a postal-address merge field in every provider e-mail with a gate check; campaign audiences count only families who opted in to "nearby"; an enquiry to an unclaimed provider becomes the sales touch "a family asked about you" and the family is told; R9 reworded, R19 with a 60-day floor, **R23** sending guard (bounce ≥ 2 % pauses, daily cap paces), R20 extended to AI-drafted messages; the propensity score gains saves/asks (+15), not-my-program (−40), bounce (−20), unsubscribe (excluded); operator hours as a modelled cost and a tile, batch approval of real-footage clips, earned auto-approval per department after four clean weeks (reminders earned, FAQ week 1 of 4); capture measured as upgrades that followed a delivered result; children appear to providers as an age band, by name only in the family's own view (**R24**); stage history logged and shown; consent proof on the preference row; the conversations line as an economics input (0 = bundled, 99 = Yelp's price); Release 1 re-cut into read-only and keyed halves; positioning against Sawyer: families, not software | owner: "based on your recommendation … update the prototype, the documentation, the business logic, the presentation and everything related" | asks #14 closed; #15 (the platform's postal address) added; audit A1–A18 all addressed except A15 and A16's platform-side halves, which are asks #12 and #5 |
| D31 | 2026-09-19 | **The platform's own privacy policy and terms (last updated 2026-09-12, read with `curl`) bind the machine**: the service is "intended for adults (18+)" and "does not knowingly collect personal information from children" → the machine stores **no child's name at all**, only ages (R24 strengthened; the family persona is now "a child of 5, a child of 9"); "we do not yet offer email alerts; if we add them, the preferences you set will be described here" → the digest and alerts need a policy update before launch (ask #16); account activity (views, saves, contacts) is recorded "only after you explicitly opt in, off by default" → campaign audiences come only from opted-in accounts (ask #17); claim and update requests are an existing platform feature (e-1) → apply-to-manage rides on it; sign-in is doneisbetter (ADR-5 confirmed); no postal address is published — the contact is info@classscout.ai → ask #15 stays open | Calvus, on the platform's own pages; owner: "please do" | audit A19 added |
| D32 | 2026-09-19 | **The responsible-data and marketing framework applies to every client** (`18-responsible-data-policy-framework.md`, research IV): one policy record per instance (jurisdictions and laws, audience model, child-data rule, minors' marketing bans and quiet hours, channels and consent basis, high-privacy defaults, cap, opt-out SLA, AI disclosure, retention, the client's published privacy-policy clauses, DPIA) and a **policy gate (R26)** in the outbox and the jobs — a feature runs only when the fields it needs are set. Ten principles, of which three are new rules: R26 the gate, R27 no profiling or targeted advertising on a minor's data even with consent, R28 high-privacy defaults. The first client is named correctly: **ClassScout**, operator of the instance **Your Field NYC**; the reference instance carries its own record. The platform's *Policy* screen is the onboarding checklist and the gate report | owner: "plan to have these kinds of policies for any other clients … a responsible system that cares about our customers' data and uses it commercially but properly and delivers business value" | asks #15–17 are gate fields; the sample invitation now stops at the gate once and explains before continuing |
| D33 | 2026-09-19 | **Two interfaces, one machine**: **Simple** — semi-automatic and guided: a *Home* screen that lists what needs a person as ranked recommendations with the reason and one button each, a "Do the recommended actions" press that runs every safe recommendation at once (approvals of posts and clips, the invitation when the gate allows, filing the radar — never a reply, never a judgement call), and a reduced navigation (Home, Approvals, Providers, Policy, How to use); **Advanced** — every screen and every input. A **What is this?** panel on every screen; a **How to use** screen (the routine, the states, the rules that never move, the screens, the documentation); a **Templates** screen (sequences, posts, campaigns, knowledge files, policy records for other client types) whose "Use" copies a template into the machine. Mode in the top bar and as `?mode=`; Simple is the default | owner: "a semi automatic simple to use client interface and advanced interface for professional users with explanations and how to use supports and templates and recommendations" | the human gate does not move: the press is still a person's |
