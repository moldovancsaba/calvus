# Drafts

One file per article, `<slug>.md`, copied from `../article-template.md`. Run `../check-draft.py` on a
draft until it prints OK, then set `status: ready-for-editor`. See `../CONTENT-HANDOVER.md` §9.
